class Subcuentum < ActiveRecord::Base
  devise :database_authenticatable,:registerable,
         :recoverable, :rememberable, :trackable, :validatable 

  attr_accessible :alias, :rfc, :password, :password_confirm, 
:tags_attributes
  belongs_to :user
end
