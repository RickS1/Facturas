<h2>Reenviar instrucciones de desbloqueo</h2>

<%= simple_form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= f.error_notification %>
  <%= f.full_error :unlock_token %>

  <div class="form-inputs">
    <%= f.input :email, :required => true, :autofocus => true, :label => 'Correo electrónico', :placeholder => 'algo123@dominio.com' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Reenviar instrucciones", :class => 'btn btn-primary' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
