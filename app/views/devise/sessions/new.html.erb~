<h2>Iniciar sesión</h2>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <%= f.error_notification %>
   <div>
    <%= f.input :rfc, :required => true, :autofocus => true, :label => "RFC", :input_html => {:maxlength =>50,:style=> 'width: 150px'} %>
    <%= f.input :password, :required => true, :label => "Contraseña", :input_html => {:maxlength =>20,:style=> 'width: 150px'} %>
   </div>
    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>

  <div class="form-actions">
    <%= f.button :submit, "Iniciar sesión", :class => 'btn btn-primary' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
