<h1>Listing subcuenta</h1>

<table>
  <tr>
    <th>Alias</th>
    <th>Password</th>
    <th>Password confirm</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subcuenta.each do |subcuentum| %>
  <tr>
    <td><%= subcuentum.alias %></td>
    <td><%= subcuentum.password %></td>
    <td><%= subcuentum.password_confirm %></td>
    <td><%= link_to 'Show', subcuentum %></td>
    <td><%= link_to 'Edit', edit_subcuentum_path(subcuentum) %></td>
    <td><%= link_to 'Destroy', subcuentum, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subcuentum', new_subcuentum_path %>
