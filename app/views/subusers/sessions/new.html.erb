<h2>Sign in</h2>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
        <%= f.input :rfc, :required => true, :autofocus => true, :label => 'RFC' %>
    <%= f.input :alias, :required => true, :autofocus => true, :label => 'Alias de la cuenta', :hint => 'Debe proporcionarlo el administrador' %>
    <%= f.input :password, :required => true, :label => 'Contraseña', :hint => 'Proporcionada por el administrador' %>
    <%= f.input :password_confirmation, :required => true %>
    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Iniciar sesión", :class => 'btn btn-primary' %>
  </div>
<% end %>

<%= render "subusers/shared/links" %>
